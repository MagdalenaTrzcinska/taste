import { NameChangePipe } from './name-change.pipe';

describe('NameChangePipe', () => {
  it('create an instance', () => {
    const pipe = new NameChangePipe();
    expect(pipe).toBeTruthy();
  });
});
